{
  "name": "@types/jscad__csg",
  "version": "0.0.1",
  "description": "Unofficial type definitions for @jscad/csg which auto-generated from original JSDoc annotations.",
  "main": "index.js",
  "scripts": {
    "copy": "cpx node_modules/@jscad/csg/{src/{api,core,fonts}/**/*.js,*.js} .",
    "clean": "rimraf src/",
    "generate": "tsc || exit 0",
    "build": "run-s clean copy generate",
    "postinstall": "postinstall-build build"
  },
  "keywords": [],
  "author": "kume-negitoro",
  "license": "WTFPL",
  "devDependencies": {
    "@jscad/csg": "^0.7.0",
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "postinstall-build": "^5.0.3"
  }
}
